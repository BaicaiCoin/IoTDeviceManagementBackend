<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.iotdevicemanagementbackend.mapper.DeviceMapper">
    <insert id="addDevice" parameterType="Device">
        insert into iot_device_management.device(device_name, type, description, user_id, create_time, last_active_time) values (#{deviceName}, #{type}, #{description}, #{userId}, #{createTime}, #{lastActiveTime});
    </insert>

</mapper>